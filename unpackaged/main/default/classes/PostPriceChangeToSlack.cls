public with sharing class PostPriceChangeToSlack {
    @InvocableMethod(label='Post Price Change to Slack' description='Posts price change information to Slack')
    public static void sendPriceChangeNotification(List<Id> propertyIds) {
        // Your logic to post to Slack
    }
}